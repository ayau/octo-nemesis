<!DOCTYPE html>
<html>
<head>
  <title>Sigma Nu Rush 2013</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
    <nav class='navbar'>
        <div class='navbar-inner'>
            <section class='container'>
                <a href='/' class='brand'>
                    <h1>Sigma Nu Rush 2013</h1>
                </a>
                <input type="text" class="input-large search-query add-or-search" placeholder="Add or search for rushes">
                <ul class='nav nav-btn'>
                    <% if signed_in? %>
                        <li><%= link_to current_user.name, current_user %></li>
                        <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
                    <% else %>
                        <li>
                            <a href='/auth/facebook'>Sign in</a>
                        </li>
                    <% end %>
                </ul>
                <ul class='nav nav-header'>
                    <li>
                        <a href='/rushes'>Rushes</a>
                    </li>
                    <li>
                        <a href='/events'>Events</a>
                    </li>
                </ul>
            </section>
        </div>
    </nav>
    <section class='container'>
        <%= yield %>
    </section>

    <footer> © 2012 Sigma Nu Beta Rho<footer>
</body>
</html>
