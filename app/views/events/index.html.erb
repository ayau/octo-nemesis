<section class='events'>
    <%= stylesheet_link_tag "application", "pages" %> 
    <center>
        <h1>Events</h1>
        <input type='button' class='add_event btn btn-large btn-primary' value='Add event' onclick='window.location = "/events/new"'/>
        <!-- <p id="notice"><%= notice %></p> -->
    </center>
<!--     <% for e in @events %>
        <h3><%= e.name %></h3>
        <% start_string = e.start_time.to_s %>
        <% end_string = e.end_time.to_s %>
        <% if !start_string.blank? && !end_string.blank? %>
        <h4><%= start_string %> to <%= end_string %></h4>
        <% end %>
        <div class='photo' style='background-image: url(<%= e.photo %>) '></div>    
        <p><%= e.description %></p>
    <% end %>
 -->
    <% for e in @events %>
        <article class='event'>
            <header>
                <h3><%= e.name %></h3>
                <input type='button' class='edit_event btn btn-success' value='edit' onclick='window.location = "/events/<%= e.id %>/edit"'/>
                <% if e.date? %>
                    <p class='date'><%= Date.strptime(e.date, '%d %B %Y').strftime('%a (%m/%d)') %></p>
                <% end %>
            </header>
            <div class='details'>
                <% if e.start_time? && e.end_time? %>
                    <p><%= e.start_time.strftime("%I:%M%p") %> - <%= e.end_time.strftime("%I:%M%p") %></p>
                <% end %>
                <p>
                    <%= e.description %>
                    <% if !e.photo? %>
                        <img class='photo' src='http://dictionaryproject.files.wordpress.com/2012/04/t300-red_solo_cup.jpg?w=500'/>
                    <% else %>
                        <img class='photo' src='<%= e.photo %>'/>
                    <% end %>
                </p>
                <%= link_to 'delete', @event, :confirm => 'Are you sure you want to delete this event?', :method => :delete, :class => 'btn-small btn btn-danger delete_event' %>
                <div class='invited' style='display: none'>
                    <p>20 rushes invited</p>
                    <ul>
                        <li><a>Brandon</a></li>
                        <li><a>Joe</a></li>
                        <li><a>Sam</a></li>
                        <li><a>Random Person</a></li>
                    </ul>
                <input type='button' class='btn btn-success' value='invite'/>
                </div>
            </div>
        </article>
    <% end %>
</section>